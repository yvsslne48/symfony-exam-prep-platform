// ══════════════════════════════════════════════════════════════
//  SPRING BOOT EXAMS DATA
// ══════════════════════════════════════════════════════════════

export const SPRING_QCM = [
  // OOP & Couplage
  { section: 'OOP & Couplage', q: 'Quelle relation UML quand A hérite de B ?', opts: ['Composition','Association','Héritage (extends)','Implémentation'], ans: 2 },
  { section: 'OOP & Couplage', q: 'Quel mot-clé Java est signe de couplage fort ?', opts: ['interface','extends','new','implements'], ans: 2 },
  { section: 'OOP & Couplage', q: 'La relation "a" faible où B peut exister sans A est ?', opts: ['Composition','Héritage','Agrégation','Implémentation'], ans: 2 },
  { section: 'OOP & Couplage', q: 'Quelle couche contient la logique métier ?', opts: ['Controller','Repository','View','Service'], ans: 3 },
  // Spring Core
  { section: 'Spring Core', q: 'Que signifie Bean dans Spring ?', opts: ['Un fichier de config','Un objet géré par Spring','Une base de données','Une interface Java'], ans: 1 },
  { section: 'Spring Core', q: '@Component peut être placé sur une interface ?', opts: ['Oui, recommandé','Non, seulement classes instanciables','Oui, pas les abstraites','Oui, toujours'], ans: 1 },
  { section: 'Spring Core', q: 'Quel est le nom de bean par défaut pour "OrderService" ?', opts: ['OrderService','order_service','ORDERSERVICE','orderService'], ans: 3 },
  { section: 'Spring Core', q: '@RestController est équivalent à ?', opts: ['@Controller seulement','@Component + @RequestMapping','@Controller + @ResponseBody','@Service + @Controller'], ans: 2 },
  { section: 'Spring Core', q: 'Quelle exception si 2 beans candidats pour une interface ?', opts: ['BeanAmbiguityException','NoUniqueBeanDefinitionException','AmbiguousBeanException','MultipleBeanException'], ans: 1 },
  // Scopes
  { section: 'Scopes & Cycle de vie', q: 'Quel est le scope par défaut d\'un bean Spring ?', opts: ['Prototype','Request','Singleton','Session'], ans: 2 },
  { section: 'Scopes & Cycle de vie', q: '@PostConstruct est une annotation de quel package ?', opts: ['org.springframework','jakarta.annotation','java.lang','javax.inject'], ans: 1 },
  { section: 'Scopes & Cycle de vie', q: '@PreDestroy est garanti à 100% d\'être exécuté ?', opts: ['Oui, toujours','Non, pas en cas de crash','Oui, sauf en production','Non, jamais'], ans: 1 },
  { section: 'Scopes & Cycle de vie', q: 'BeanFactoryPostProcessor s\'exécute à quel moment ?', opts: ['Après création des beans','Avant instanciation des beans','Pendant la requête HTTP','Lors de la destruction'], ans: 1 },
  // Configuration
  { section: 'Configuration', q: 'Comment lire la propriété "app.name" avec une annotation ?', opts: ['@Property("app.name")','@Config("app.name")','@Value("${app.name}")','@Load("app.name")'], ans: 2 },
  { section: 'Configuration', q: '@ConfigurationProperties(prefix="db") mappe quelles propriétés ?', opts: ['Toutes les propriétés','Propriétés commençant par "db."','Seulement "db"','Fichier db.properties'], ans: 1 },
  { section: 'Configuration', q: 'Pour @ConfigurationProperties, quoi est obligatoire ?', opts: ['@Autowired','Constructeur sans args','Getters et Setters','@Component seulement'], ans: 2 },
  // JDBC
  { section: 'JDBC & BDD', q: 'Avantage principal de JdbcTemplate vs Raw JDBC ?', opts: ['Plus rapide','Moins de boilerplate, gestion auto ressources','Plus compatible','Meilleure sécurité'], ans: 1 },
  { section: 'JDBC & BDD', q: 'Quelle méthode JdbcTemplate pour UN seul résultat ?', opts: ['query()','queryForList()','queryForObject()','queryForMap()'], ans: 2 },
  { section: 'JDBC & BDD', q: 'Quelle méthode JdbcTemplate pour INSERT/UPDATE/DELETE ?', opts: ['execute()','modify()','update()','write()'], ans: 2 },
  { section: 'JDBC & BDD', q: 'Quelle interface Spring Data JPA pour CRUD complet ?', opts: ['CrudRepository','SimpleRepository','JpaRepository','BaseRepository'], ans: 2 },
  { section: 'JDBC & BDD', q: 'Un RowMapper sert à ?', opts: ['Créer des tables SQL','Transformer ResultSet en objet Java','Gérer les transactions','Valider les données'], ans: 1 },
  // REST API
  { section: 'REST API', q: 'Code HTTP pour créer une ressource (POST) ?', opts: ['200 OK','201 Created','202 Accepted','204 No Content'], ans: 1 },
  { section: 'REST API', q: 'Code HTTP pour DELETE réussi sans corps ?', opts: ['200 OK','201 Created','204 No Content','404 Not Found'], ans: 2 },
  { section: 'REST API', q: '@PathVariable est pour ?', opts: ['Paramètres GET (?key=val)','Corps JSON','Segment d\'URL (/users/{id})','Headers HTTP'], ans: 2 },
  { section: 'REST API', q: 'POST est idempotent ?', opts: ['Oui, comme DELETE','Non, crée une nouvelle ressource à chaque appel','Oui, comme GET','Oui, si données identiques'], ans: 1 },
  { section: 'REST API', q: '@RestControllerAdvice sert à ?', opts: ['Configurer les routes','Gérer les exceptions globalement','Créer des beans','Accéder à la BDD'], ans: 1 },
]

export const SPRING_PDF = [
  // OOP
  { section: 'OOP & Couplage', type: 'open',
    q: 'Expliquer la différence entre Agrégation et Composition avec un exemple concret.',
    ans: 'Agrégation (lien faible ◇) : B peut exister sans A. Ex: Team a des Players, les joueurs peuvent exister sans l\'équipe.\nComposition (lien fort ◆) : B ne peut PAS exister sans A. Ex: House a des Rooms, la chambre n\'existe pas sans la maison.\nRègle : si on supprime A, est-ce que B survit ? Oui = Agrégation, Non = Composition.',
    hint: 'Agrégation ◇ = faible (B survit). Composition ◆ = fort (B disparaît avec A).' },
  { section: 'OOP & Couplage', type: 'dropdown',
    q: 'Le mot-clé "new" dans le code d\'une classe signifie systématiquement un couplage fort',
    opts: ['Vrai','Faux'], ans: 0,
    hint: 'Vrai. new = instanciation directe = couplage fort.' },
  { section: 'OOP & Couplage', type: 'open',
    q: 'Comment peut-on rompre un couplage fort entre A et B ? Expliquer la solution.',
    ans: 'Introduire une interface I entre A et B :\n1. Créer une interface I avec les méthodes dont A a besoin\n2. A dépend de I (pas de B directement)\n3. B implémente I\n4. Spring injecte B dans A via @Autowired',
    hint: 'Introduire interface I → A dépend de I → B implémente I → Spring DI injecte B.' },
  // Spring Core
  { section: 'Spring Core — Beans & IoC', type: 'open',
    q: 'Quelle est la différence entre @Component, @Service et @Repository ?',
    ans: '@Component = déclaration générique de bean Spring\n@Service = @Component avec rôle sémantique "couche logique métier"\n@Repository = @Component + conversion des exceptions SQL en DataAccessException (unchecked)',
    hint: 'Tous = @Component. @Repository ajoute la conversion des exceptions SQL.' },
  { section: 'Spring Core — Beans & IoC', type: 'dropdown',
    q: 'Les annotations Spring peuvent causer des erreurs à la compilation',
    opts: ['Vrai','Faux'], ans: 1,
    hint: 'Faux. Les annotations Spring sont évaluées SEULEMENT à l\'exécution (runtime).' },
  { section: 'Spring Core — Beans & IoC', type: 'open',
    q: 'On a B et C qui implémentent I, et A qui a besoin de I. Que se passe-t-il ? Comment résoudre ?',
    ans: 'Spring lève une NoUniqueBeanDefinitionException au démarrage car il trouve 2 beans candidats.\n\nSolutions :\n1. @Qualifier("nomBean") sur le paramètre du constructeur\n2. @Primary sur B ou C pour indiquer la priorité',
    hint: 'NoUniqueBeanDefinitionException. Solutions : @Qualifier ou @Primary.' },
  { section: 'Spring Core — Beans & IoC', type: 'checkbox',
    q: 'Quand dit-on qu\'une classe est un bean Spring ?',
    opts: ['La classe est annotée avec @Component (ou stéréotype)','La classe est dans un package scanné par @ComponentScan','La classe étend AbstractBean','La classe est dans le pom.xml'],
    ans: [0,1],
    hint: 'Un bean = annoté @Component ET dans le package scanné. Les 2 conditions.' },
  // Scopes
  { section: 'Scopes & Cycle de vie', type: 'open',
    q: 'Qu\'est-ce que le scope Singleton en Spring ? Quel problème peut-il poser ?',
    ans: 'Singleton = une seule instance du bean créée dans tout le contexte. Tous les beans partagent la MÊME instance.\n\nProblème : Si le bean a des attributs (état), les modifications faites par A seront visibles par B et C → corruption d\'état.\n\nSolution : Utiliser Prototype si le bean a un état.',
    hint: 'Singleton = partagé = problème si le bean a un état.' },
  { section: 'Scopes & Cycle de vie', type: 'dropdown',
    q: 'Avec scope Singleton et @PostConstruct, si A et B injectent C, combien de fois init() sera appelé ?',
    opts: ['0 fois','1 fois','2 fois','3 fois'], ans: 1,
    hint: 'Singleton = 1 seule instance = init() appelé 1 fois seulement.' },
  { section: 'Scopes & Cycle de vie', type: 'open',
    q: '@PostConstruct public void destroy() { ... } — Quand s\'exécute cette méthode ?',
    ans: 'À l\'INITIALISATION du bean (après sa création et l\'injection des dépendances).\n\nPiège classique : le nom "destroy" est trompeur, mais c\'est l\'ANNOTATION qui détermine le comportement.\n@PostConstruct = après création, PAS à la destruction !',
    hint: 'L\'annotation prime sur le nom ! @PostConstruct = initialisation, même si la méthode s\'appelle destroy.' },
  // JDBC
  { section: 'JDBC & Base de données', type: 'open',
    q: 'Quelles sont les méthodes principales de JdbcTemplate et leurs usages ?',
    ans: 'queryForObject(sql, Type.class) → UN seul scalaire (ex: COUNT(*))\nqueryForObject(sql, rowMapper, params) → UN seul objet\nquery(sql, rowMapper) → LISTE d\'objets\nupdate(sql, params) → INSERT, UPDATE, DELETE\nbatchUpdate(sql, batchArgs) → plusieurs INSERTs en lot',
    hint: 'queryForObject=1 résultat, query=liste, update=écriture.' },
  { section: 'JDBC & Base de données', type: 'dropdown',
    q: 'JdbcTemplate gère automatiquement la fermeture de la connexion JDBC',
    opts: ['Vrai','Faux'], ans: 0,
    hint: 'Vrai. JdbcTemplate gère automatiquement connexion, fermeture et exceptions.' },
  { section: 'JDBC & Base de données', type: 'open',
    q: 'Qu\'est-ce qu\'un RowMapper ? Créer un exemple pour une entité User avec id et firstName.',
    ans: 'Un RowMapper est une interface Spring qui définit comment transformer une ligne ResultSet SQL en objet Java.\n\npublic class UserRowMapper implements RowMapper<User> {\n    @Override\n    public User mapRow(ResultSet rs, int rowNum) throws SQLException {\n        User user = new User();\n        user.setId(rs.getLong("id"));\n        user.setFirstName(rs.getString("first_name"));\n        return user;\n    }\n}\n\nUtilisation : jdbcTemplate.query("SELECT * FROM users", new UserRowMapper());',
    hint: 'RowMapper<T> avec mapRow(ResultSet rs, int rowNum) throws SQLException.' },
  // REST API
  { section: 'REST API Spring Boot', type: 'open',
    q: 'Quelles sont les bonnes pratiques des URLs REST ? Donner des exemples corrects et incorrects.',
    ans: '1. Noms (ressources) pas verbes : ✅ POST /users vs ❌ POST /createUser\n2. Pluriel et minuscules : ✅ /users vs ❌ /User\n3. Versionner : ✅ /api/v1/users\n4. Ressources imbriquées : ✅ /users/5/orders\n5. Query params pour filtres : ✅ /users?name=Yassine&page=0',
    hint: 'Noms pluriels, pas de verbes, versionner /api/v1/...' },
  { section: 'REST API Spring Boot', type: 'dropdown',
    q: 'POST est une opération idempotente',
    opts: ['Vrai','Faux'], ans: 1,
    hint: 'Faux. POST crée une nouvelle ressource à chaque appel → NON idempotent.' },
  { section: 'REST API Spring Boot', type: 'open',
    q: 'À quoi sert @RestControllerAdvice ? Donner un exemple de handler.',
    ans: '@RestControllerAdvice centralise la gestion des exceptions pour toute l\'API. Avantages : pas de try/catch dans les contrôleurs.\n\n@RestControllerAdvice\npublic class GlobalExceptionHandler {\n    @ExceptionHandler(ResourceNotFoundException.class)\n    public ResponseEntity<ErrorResponse> handleNotFound(ResourceNotFoundException ex) {\n        return ResponseEntity.status(404).body(new ErrorResponse(404, ex.getMessage()));\n    }\n}',
    hint: '@RestControllerAdvice + @ExceptionHandler(Type.class) = gestion centralisée.' },
]

export const SPRING_REAL = [
  { section: '🔗 OOP & Couplage (5pts)', type: 'open',
    q: '1. Expliquer la différence entre Agrégation et Composition avec un exemple. (2pts)',
    ans: 'Agrégation ◇ (lien faible) : B peut exister sans A. Ex: Team a des Players — les joueurs existent sans l\'équipe.\nComposition ◆ (lien fort) : B ne peut PAS exister sans A. Ex: House a des Rooms — la chambre n\'existe pas sans la maison.\n\nRègle : Si on supprime A, est-ce que B survit ? Oui = Agrégation, Non = Composition.',
    hint: 'Agrégation = faible (◇). Composition = fort (◆).' },
  { section: '🔗 OOP & Couplage (5pts)', type: 'open',
    q: '2. Comment rompre un couplage fort entre A et B ? (2pts)',
    ans: '1. Créer une interface I définissant le contrat\n2. A dépend de I (pas de B directement)\n3. B implémente I\n4. Spring injecte B dans A automatiquement\n\nRésultat : A ne connaît plus B, seulement I.',
    hint: 'Interface + DI Spring.' },
  { section: '🔗 OOP & Couplage (5pts)', type: 'open',
    q: '3. Quelle couche en architecture 3-tiers contient la logique métier ? (1pt)',
    ans: 'La couche Service (@Service). Elle est placée entre le Controller (exposition HTTP) et le Repository (accès BDD). Elle contient toutes les règles fonctionnelles de l\'application.',
    hint: '@Service = logique métier.' },
  { section: '🌱 Spring Core — Beans & IoC (8pts)', type: 'open',
    q: '4. Qu\'est-ce qu\'un Bean Spring ? Comment le déclarer ? (2pts)',
    ans: 'Un Bean Spring est un objet dont le cycle de vie (création, injection, destruction) est géré par Spring.\n\nDéclaration :\n@Component → bean générique\n@Service → couche logique métier\n@Repository → couche accès données\n@RestController → contrôleur REST\n\nSpring instancie et injecte ces beans automatiquement.',
    hint: '@Component ou stéréotype (@Service, @Repository...).' },
  { section: '🌱 Spring Core — Beans & IoC (8pts)', type: 'open',
    q: '5. Qu\'est-ce que l\'Autowiring ? Comment fonctionne-t-il ? (2pts)',
    ans: 'L\'Autowiring permet à Spring d\'injecter automatiquement les dépendances en analysant les type hints PHP des arguments du constructeur.\n\nExemple :\npublic class OrderService {\n    public OrderService(IOrderRepo repo) { // Spring injecte automatiquement\n        this.repo = repo;\n    }\n}\n\n@Autowired est optionnel si 1 seul constructeur (Spring 4.3+).',
    hint: 'Injection auto via type hints du constructeur.' },
  { section: '🌱 Spring Core — Beans & IoC (8pts)', type: 'open',
    q: '6. Deux classes B et C implémentent I. A a besoin de I. Problème ? Solution ? (2pts)',
    ans: 'Problème : NoUniqueBeanDefinitionException — Spring ne sait pas lequel de B ou C injecter pour I.\n\nSolutions :\n1. @Qualifier("nomBean") sur le constructeur de A\n2. @Primary sur B ou C pour le rendre prioritaire',
    hint: 'NoUniqueBeanDefinitionException → @Qualifier ou @Primary.' },
  { section: '🌱 Spring Core — Beans & IoC (8pts)', type: 'open',
    q: '7. Différence entre @PostConstruct et @PreDestroy ? (2pts)',
    ans: '@PostConstruct : s\'exécute APRÈS la création du bean et l\'injection de toutes ses dépendances. Utile pour initialiser des ressources.\n\n@PreDestroy : s\'exécute AVANT la destruction du bean. Non garanti en cas de crash ou coupure d\'alimentation.\n\n⚠️ Piège : le nom de la méthode n\'a aucune importance, c\'est l\'annotation qui compte.',
    hint: '@PostConstruct = après init. @PreDestroy = avant destruction (non garanti).' },
  { section: '🔄 Scopes & Cycle de vie (4pts)', type: 'open',
    q: '8. Expliquer les scopes Singleton et Prototype. Quand utiliser chacun ? (2pts)',
    ans: 'Singleton (défaut) : UNE seule instance partagée dans tout le contexte.\n→ Utiliser pour les services SANS état (stateless) : OrderService, UserService...\n\nPrototype : NOUVELLE instance créée à chaque injection.\n→ Utiliser si le bean a un état (attributs qui peuvent être modifiés).',
    hint: 'Singleton = partagé (stateless). Prototype = une instance par injection (avec état).' },
  { section: '🔄 Scopes & Cycle de vie (4pts)', type: 'checkbox',
    q: '9. Quelles affirmations sur les scopes sont VRAIES ? (2pts)',
    opts: ['Singleton = 1 instance partagée dans le contexte','Prototype = 1 instance par injection','Session = 1 instance par session HTTP','Tous les beans sont Singleton par défaut','@Scope("singleton") est obligatoire pour Singleton'],
    ans: [0,1,2,3],
    hint: 'Singleton, Prototype, Session sont corrects. @Scope("singleton") est optionnel (c\'est le défaut).' },
  { section: '🗃️ JDBC & Base de données (6pts)', type: 'open',
    q: '10. Pourquoi utilise-t-on JdbcTemplate au lieu du Raw JDBC ? (2pts)',
    ans: 'Raw JDBC nécessite beaucoup de code répétitif (boilerplate) : ouvrir connexion, créer PreparedStatement, itérer ResultSet, fermer connexion...\n\nJdbcTemplate simplifie tout :\n- Gère automatiquement la connexion et sa fermeture\n- Convertit les SQLException en DataAccessException (unchecked)\n- Réduit le code à l\'essentiel : queryForObject(), query(), update()',
    hint: 'Moins de boilerplate, gestion auto ressources, exceptions unchecked.' },
  { section: '🗃️ JDBC & Base de données (6pts)', type: 'open',
    q: '11. Créer un exemple de RowMapper pour une entité Product avec id, name, price. (2pts)',
    ans: 'public class ProductRowMapper implements RowMapper<Product> {\n    @Override\n    public Product mapRow(ResultSet rs, int rowNum) throws SQLException {\n        Product p = new Product();\n        p.setId(rs.getLong("id"));\n        p.setName(rs.getString("name"));\n        p.setPrice(rs.getDouble("price"));\n        return p;\n    }\n}',
    hint: 'implements RowMapper<T> + mapRow(ResultSet, int) throws SQLException.' },
  { section: '🗃️ JDBC & Base de données (6pts)', type: 'open',
    q: '12. Quelle est la différence entre JdbcTemplate et Spring Data JPA ? (2pts)',
    ans: 'JdbcTemplate : proche du SQL natif. On écrit les requêtes SQL manuellement. Bon pour les requêtes complexes ou l\'optimisation fine.\n\nSpring Data JPA : abstraction plus haute. On définit des interfaces, Spring génère les requêtes automatiquement via la convention de nommage (findByName, findByAgeGreaterThan...). Plus rapide à développer.',
    hint: 'JdbcTemplate = SQL manuel. Spring Data JPA = requêtes auto par convention.' },
  { section: '🌐 REST API Spring Boot (7pts)', type: 'open',
    q: '13. Expliquer les 5 méthodes HTTP REST et leur idempotence. (2pts)',
    ans: 'GET → Lire (idempotent)\nPOST → Créer (NON idempotent)\nPUT → Remplacer entièrement (idempotent)\nPATCH → Modifier partiellement (idempotent)\nDELETE → Supprimer (idempotent)\n\nIdempotent = même résultat si appelé plusieurs fois.',
    hint: 'POST est le seul NON idempotent.' },
  { section: '🌐 REST API Spring Boot (7pts)', type: 'open',
    q: '14. Écrire un contrôleur REST complet pour une entité User : GET /users, GET /users/{id}, POST /users, DELETE /users/{id}. (3pts)',
    ans: '@RestController\n@RequestMapping("/api/v1/users")\npublic class UserController {\n    @GetMapping\n    public ResponseEntity<List<User>> getAll() {\n        return ResponseEntity.ok(userService.findAll()); // 200\n    }\n    @GetMapping("/{id}")\n    public ResponseEntity<User> getById(@PathVariable Long id) {\n        return userService.findById(id)\n            .map(ResponseEntity::ok)\n            .orElse(ResponseEntity.notFound().build()); // 404\n    }\n    @PostMapping\n    public ResponseEntity<User> create(@RequestBody UserDTO dto) {\n        User saved = userService.save(dto);\n        return ResponseEntity.status(201).body(saved); // 201\n    }\n    @DeleteMapping("/{id}")\n    public ResponseEntity<Void> delete(@PathVariable Long id) {\n        userService.delete(id);\n        return ResponseEntity.noContent().build(); // 204\n    }\n}',
    hint: '@GetMapping, @PostMapping, @DeleteMapping + ResponseEntity avec bons codes HTTP.' },
  { section: '🌐 REST API Spring Boot (7pts)', type: 'open',
    q: '15. À quoi sert @RestControllerAdvice ? Comment gérer une exception ResourceNotFoundException ? (2pts)',
    ans: '@RestControllerAdvice centralise la gestion des exceptions pour toute l\'API. Avantages : code DRY, réponses d\'erreur uniformes.\n\n@RestControllerAdvice\npublic class GlobalExceptionHandler {\n    @ExceptionHandler(ResourceNotFoundException.class)\n    public ResponseEntity<ErrorResponse> handleNotFound(ResourceNotFoundException ex) {\n        return ResponseEntity.status(404)\n            .body(new ErrorResponse(404, ex.getMessage()));\n    }\n}',
    hint: '@RestControllerAdvice + @ExceptionHandler(Type.class).' },
]

// ── Examen Réel — Version 2 (2026-2027) — Développement avancé JEE ──
export const SPRING_REAL_2027 = [
  { section: '🅰️ Spring IoC & Beans (Partie A)', type: 'open',
    q: '1. Avec @Service sur PaiementVisaService et PaiementVirementService, expliquer le résultat de l\'exécution du main() (création du contexte, récupération du bean PaiementService, appel de payer(1500.0)). (2pts)',
    ans: 'Au lancement, AnnotationConfigApplicationContext scanne le package "ma.exemple.spring" (@ComponentScan) et enregistre tous les beans annotés (@Service, @Component...). Ici, DEUX classes (PaiementVisaService et PaiementVirementService) implémentent PaiementService et sont toutes deux des @Service.\n\nQuand on appelle context.getBean(PaiementService.class), Spring trouve 2 candidats pour l\'interface PaiementService → exception NoUniqueBeanDefinitionException levée au moment du getBean(). "H" s\'affiche, puis "D" après création du contexte, puis l\'exception est levée avant "T" et "E" : le programme s\'arrête en erreur.',
    hint: '2 beans implémentent la même interface → NoUniqueBeanDefinitionException sur getBean(PaiementService.class).' },
  { section: '🅰️ Spring IoC & Beans (Partie A)', type: 'open',
    q: '2. Créer une nouvelle implémentation PaiementALaReceptionService puis adapter le code afin d\'utiliser cette nouvelle implémentation. (2pts)',
    ans: '@Service\nclass PaiementALaReceptionService implements PaiementService {\n    @Override\n    public void payer(double montant) {\n        System.out.println("Paiement à la réception : " + montant + " DH");\n    }\n}\n\nPour résoudre l\'ambiguïté (3 beans implémentent PaiementService), récupérer le bean par son nom ou utiliser @Qualifier/@Primary :\n\nPaiementService paiementService = context.getBean("paiementALaReceptionService", PaiementService.class);',
    hint: 'Nouvelle classe @Service implements PaiementService + @Qualifier/@Primary pour lever l\'ambiguïté.' },
  { section: '🅱️ API REST Produits (Partie B)', type: 'open',
    q: '3. Création réussie d\'un produit : Méthode/URL, Body requête, Statut réponse, Body réponse. (1pt)',
    ans: 'Méthode et URL : POST /api/produits\nBody de la requête : { "libelle": "Clavier mécanique" }\nStatut de la réponse : 201 Created\nBody de la réponse : { "id": 1, "libelle": "Clavier mécanique" }',
    hint: 'POST /api/produits → 201 Created + ressource créée (avec id généré).' },
  { section: '🅱️ API REST Produits (Partie B)', type: 'open',
    q: '4. Rejet de la création d\'un produit à cause de l\'absence du libellé : Méthode/URL, Body requête, Statut réponse, Body réponse. (1pt)',
    ans: 'Méthode et URL : POST /api/produits\nBody de la requête : { "libelle": "" } ou { }\nStatut de la réponse : 400 Bad Request\nBody de la réponse : { "error": "Le champ libelle est obligatoire" }',
    hint: 'Données invalides (validation @NotBlank) → 400 Bad Request avec message d\'erreur.' },
  { section: '🅱️ API REST Produits (Partie B)', type: 'open',
    q: '5. Récupération réussie d\'un produit par identifiant : Méthode/URL, Body requête, Statut réponse, Body réponse. (1pt)',
    ans: 'Méthode et URL : GET /api/produits/{id} (ex: GET /api/produits/1)\nBody de la requête : (aucun)\nStatut de la réponse : 200 OK\nBody de la réponse : { "id": 1, "libelle": "Clavier mécanique" }',
    hint: 'GET /api/produits/{id} → 200 OK + ressource.' },
  { section: '🅱️ API REST Produits (Partie B)', type: 'open',
    q: '6. Récupération réussie d\'un produit par libellé : Méthode/URL, Body requête, Statut réponse, Body réponse. (1pt)',
    ans: 'Méthode et URL : GET /api/produits?libelle=Clavier mécanique (paramètre de requête)\nBody de la requête : (aucun)\nStatut de la réponse : 200 OK\nBody de la réponse : [ { "id": 1, "libelle": "Clavier mécanique" } ] (tableau, recherche par critère)',
    hint: 'GET /api/produits?libelle=... → 200 OK + tableau de résultats (filtre/recherche).' },
  { section: '🅱️ API REST Produits (Partie B)', type: 'open',
    q: '7. Modification réussie d\'un produit : Méthode/URL, Body requête, Statut réponse, Body réponse. (1pt)',
    ans: 'Méthode et URL : PUT /api/produits/{id} (ex: PUT /api/produits/1)\nBody de la requête : { "libelle": "Clavier mécanique RGB" }\nStatut de la réponse : 200 OK\nBody de la réponse : { "id": 1, "libelle": "Clavier mécanique RGB" }',
    hint: 'PUT /api/produits/{id} → 200 OK + ressource mise à jour (idempotent).' },
  { section: '🅱️ API REST Produits (Partie B)', type: 'open',
    q: '8. Rejet de la modification à cause de la non-existence du produit : Méthode/URL, Body requête, Statut réponse, Body réponse. (1pt)',
    ans: 'Méthode et URL : PUT /api/produits/{id} (ex: PUT /api/produits/999)\nBody de la requête : { "libelle": "Clavier mécanique RGB" }\nStatut de la réponse : 404 Not Found\nBody de la réponse : { "error": "Produit 999 introuvable" }',
    hint: 'Ressource inexistante → 404 Not Found, géré via @RestControllerAdvice + ResourceNotFoundException.' },
  { section: '🅱️ API REST Produits (Partie B)', type: 'open',
    q: '9. Rejet de la création d\'un produit car la connexion BDD est KO : Méthode/URL, Body requête, Statut réponse, Body réponse. (1pt)',
    ans: 'Méthode et URL : POST /api/produits\nBody de la requête : { "libelle": "Clavier mécanique" }\nStatut de la réponse : 503 Service Unavailable (ou 500 Internal Server Error)\nBody de la réponse : { "error": "Service temporairement indisponible, veuillez réessayer plus tard" }',
    hint: 'Erreur infrastructure (BDD down) → 503/500, jamais une erreur 4xx liée au client.' },
  { section: '🅱️ API REST Produits (Partie B)', type: 'open',
    q: '10. Désactivation d\'un produit : Méthode/URL, Body requête, Statut réponse, Body réponse. (1pt)',
    ans: 'Méthode et URL : PATCH /api/produits/{id} (ex: PATCH /api/produits/1)\nBody de la requête : { "actif": false }\nStatut de la réponse : 200 OK\nBody de la réponse : { "id": 1, "libelle": "Clavier mécanique", "actif": false }\n\nPATCH (modification partielle) plutôt que PUT (remplacement complet) ou DELETE (suppression).',
    hint: 'PATCH /api/produits/{id} avec { "actif": false } → 200 OK (modification partielle, pas suppression).' },
  { section: '🅲 JPA & HQL (Partie C)', type: 'open',
    q: '11. Compléter les annotations de mapping JPA manquantes pour mapper PaimentEntity sur la table Paiment (colonnes Id_paiment, Montant_paiment). (1pt)',
    ans: '@Entity\n@Table(name = "Paiment")\npublic class PaimentEntity {\n\n    @Id\n    @GeneratedValue(strategy = GenerationType.IDENTITY)\n    @Column(name = "Id_paiment")\n    private Long id;\n\n    @Column(name = "Montant_paiment")\n    private Double montant;\n\n    // getters / setters\n}',
    hint: '@Entity, @Table(name=...), @Id + @GeneratedValue, @Column(name=...) pour mapper les noms de colonnes.' },
  { section: '🅲 JPA & HQL (Partie C)', type: 'open',
    q: '12. Proposer une requête HQL permettant de récupérer les paiements dont le montant est supérieur à 1000. (1pt)',
    ans: 'SELECT p FROM PaimentEntity p WHERE p.montant > 1000\n\n(HQL utilise le nom de la classe entité et de ses attributs Java, pas les noms de table/colonnes SQL.)',
    hint: 'SELECT p FROM PaimentEntity p WHERE p.montant > 1000 — HQL = entités/attributs Java, pas table/colonnes SQL.' },
]
